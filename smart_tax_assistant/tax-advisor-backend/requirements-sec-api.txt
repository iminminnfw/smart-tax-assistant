# SEC API Integration Requirements
# Install with: pip install -r requirements-sec-api.txt

# HTTP Client with async support
httpx>=0.27.0

# Redis for caching (optional but recommended)
redis>=5.0.0

# AI/LLM Support
openai>=1.12.0
anthropic>=0.18.0

# Already in main requirements.txt (if not, add these):
# fastapi>=0.115.0
# uvicorn>=0.32.0
# python-dotenv>=1.0.0
